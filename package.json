{
  "name": "conforta-store",
  "version": "1.0.0",
  "description": "Conforta Store - E-commerce with Supabase + Asaas payments",
  "scripts": {
    "start": "node scripts/dev-server.cjs",
    "dev": "node scripts/dev-server.cjs",
    "dev:static": "echo [AVISO] dev:static NAO serve /api/* — pagamentos exigem npm run dev && npx serve . --listen 3000",
    "dev:vercel": "npx vercel dev"
  }
}
